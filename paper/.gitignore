*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
*.pdf
# The built paper is committed so it can be read without a TeX toolchain;
# rebuild with `make` before committing source changes.
!main.pdf
arxiv-build/
arxiv.tar.gz
.latexindent-cruft/
indent.log
